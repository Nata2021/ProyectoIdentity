@model ProyectoIdentity.Models.Domain.Abogado

@{
    ViewData["Title"] = "Eliminar Abogado";
}

<h1 class="mb-4 text-danger">Eliminar Abogado</h1>

<h3 class="mb-3">¿Está seguro de que desea **desactivar** este abogado?</h3>
<div class="alert alert-warning mb-4" role="alert">
    Al desactivar un abogado, no podrá iniciar sesión y ya no aparecerá en las listas activas, pero sus datos históricos (casos, tareas, etc.) se conservarán.
</div>

<div class="card shadow-sm">
    @* Encapsular el contenido en una tarjeta *@
    <div class="card-header bg-dark text-white">
        <h5 class="mb-0">Información del Abogado a Desactivar</h5>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.NombreCompleto)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.NombreCompleto)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Matricula)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Matricula)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.AreaEspecializacion)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.AreaEspecializacion)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Email)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Email)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Telefono)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Telefono)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Activo)
            </dt>
            <dd class="col-sm-9">
                @if (Model.Activo)
                {
                    <span class="badge bg-success">Activo</span>
                }
                else
                {
                    <span class="badge bg-danger">Inactivo</span>
                }
            </dd>
        </dl>
    </div>
    <div class="card-footer d-flex justify-content-between">
        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="AbogadoId" />
            <button type="submit" value="Eliminar" class="btn btn-danger me-2">
                <i class="bi bi-person-x-fill me-1"></i> Confirmar Desactivar
            </button>
        </form>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-x-circle me-1"></i> Cancelar
        </a>
    </div>
</div>